import type { ChartTemplateProps } from "./types";

export const ChartTemplateWithOutData = ({
  chartId,
  dataSource,
  query,
}: ChartTemplateProps) => {
  return <div>ChartTemplateWithOutData</div>;
};
